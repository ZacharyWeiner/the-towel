<div class='container'>
<%= form_with(model: @event_comment, local: true, url: event_event_comments_path(@event, @event_comment)) do |form| %>
  <% if @event_comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@event_comment.errors.count, "error") %> prohibited this event_comment from being saved:</h2>

      <ul>
      <% @event_comment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <%= form.hidden_field :event_id, :value => @event.id%>
  <%= form.hidden_field :user_id, :value => current_user.id%>
  <%= form.hidden_field :cohort_id, :value => @event.cohort.id%>

  <div class="form-group" style='width:100%'>
    <%= form.label :comment %>
    <%= form.text_area :comment, id: :event_comment_comment, class: 'form-control' %>
  </div>

  <div class="actions">
    <%= form.submit class: 'btn btn-primary btn-block' %>
  </div>
<% end %>

</div>
