<div class='container'>
<div class="list-group">
  <% @cohort.users.each do |user| %>
  <%= link_to "#{user.email}", show_user_path(user), class: 'list-group-item list-group-item-action' %>
  <% end %>
</div>
</div>