<div class='container'>
  <div class="list-group">
  <% @cohort.events.order(date: :desc).each do |event| %>
    <%= link_to "#{event.title}", event_path(event), class: 'list-group-item list-group-item-action' %>
  <% end %>
  </div>
</div>